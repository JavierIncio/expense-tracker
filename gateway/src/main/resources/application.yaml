server:
  port: 8080

spring:
  application:
    name: gateway
  config:
    import: optional:file:.env[.properties]
  cloud:
    gateway:
      server:
        webmvc:
          routes:
            - id: identity-service
              uri: ${IDENTITY_SERVICE_URI:http://localhost:8081}
              predicates:
                - Path=/api/auth/**
            - id: expense-service
              uri:  ${EXPENSE_SERVICE_URI:http://localhost:8082}
              predicates:
                - Path=/api/expenses/**

management:
  endpoint:
    health:
      show-details: always

app:
  jwt:
    secret: ${APP_JWT_SECRET}
    issuer: exptrack-identity